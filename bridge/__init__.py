"""remarkable-bridge — host-side daemon bridging a reMarkable tablet to the Claude CLI.

Stage 1 walking skeleton: the `review` route, end to end (typed-text / single-page subset).
"""

__version__ = "0.1.0"
